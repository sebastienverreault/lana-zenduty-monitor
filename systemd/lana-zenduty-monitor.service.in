[Unit]
Description=LANA Zenduty monitor poll

[Service]
Type=oneshot
WorkingDirectory=@REPO_DIR@
ExecStart=/usr/bin/env zsh -lc 'cd @REPO_DIR@ && set -a && [ ! -f .env ] || source .env && set +a && python3 -m lana_zenduty_monitor poll'
